pseudo code
backtrack(i)
	for c in [a,b,c]
		if valid (no adj substring are equals with length in [1,i/2]
			config[i] := c
			count[i] += (c = 'c') ? 1 : 0
			if count[i] < minCount
				if i = n 
					accept config
				else 
					backtrack(i+1)
			