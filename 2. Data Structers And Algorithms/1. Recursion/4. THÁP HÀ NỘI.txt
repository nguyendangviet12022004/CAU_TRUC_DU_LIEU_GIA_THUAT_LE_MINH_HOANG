Ý TƯỞNG :
CHUYỂN N-1 ĐĨA TỪ A -> B, CHUYỂN ĐĨA NẰM DƯỚI CÙNG SANG C, SAU ĐÓ CHUYỂN NỐT (N-1) TỪ B -> C

GIÃ MÃ:
	IF N = 1
		IN RA LỆNH 
	ELSE
		DI CHUYỂN(N-1,X,(6-X-Y))
		DI CHUYỂN(1,X,Y)
		DI CHUYỂN(N-1,(6-X-Y),Y)
	